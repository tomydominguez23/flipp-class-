# FLIP CLASS — App (Vite + React + TS + Tailwind)

Portal web tipo Skool (demo) para **FLIP CLASS**.

## Comandos

```bash
npm install
npm run dev
```

## Rutas

- `/`: landing
- `/planes`: planes
- `/login` y `/registro`: acceso (demo)
- `/app`: portal (protegido por sesión en `localStorage`)
